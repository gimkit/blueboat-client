// import { StateContainer } from '@gamestdio/state-listener'

// const container = new StateContainer({})

// container.listen('messages/:index', change => console.log(change))

// container.set({
//   messages: [{ message: 'Hi there!', senderId: 'test' }]
// })

// setTimeout(() => {
//   container.set({
//     messages: [
//       { message: 'Hi there!', senderId: 'test' },
//       { message: 'Woah!', senderId: 'wow' }
//     ]
//   })
// }, 5000)
